version: 1.0
runtime: python311

build:
  commands:
    build:
      - echo "Build stage complete"

run:
  runtime-version: 3.11
  command: pip3 install -r requirements.txt && python3 -m uvicorn main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000